At the moment this project is hard-coded to use s3 to store the json files this thing uses and creates.

You'll need to add your own AWS keys and s3 bucket to the two s3 python files in the init function.
